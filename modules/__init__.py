"""Each submodule in this directory corresponds to a major functionality of the smart lamp,
   like movement control (movement_control/),
   camera processing (camera_processing/),
   and hand tracking (hand_tracking/).
   These modules encapsulate their respective logic and can operate independently."""
